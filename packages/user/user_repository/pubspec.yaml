name: user_repository
description: A repository that handles user related requests.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  user_api:
    path: ../user_api
  user_database_service:
    path: ../user_database_service
  user_sync_service:
    path: ../user_sync_service

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
